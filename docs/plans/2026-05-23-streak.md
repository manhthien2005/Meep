# Plan: Streak / Kỷ niệm Module

> **Spec:** `docs/specs/2026-05-22-streak.md` (v2, 2026-06-04)
> **Owner:** HanDHG
> **Branch:** `feature/HanDHG/streak-module`
> **Tier:** T1 Stretch — M3 ship target
> **Phụ thuộc:** Feed module (Post model + coverImageUrl), Profile module (PhotoDetailScreen + SharePhotoSheet sẽ move lên shared), Auth (currentUserProfileProvider)
> **Được phụ thuộc bởi:** (không)

---

## PART 1 — Contract artifacts (leader update vào `develop` TRƯỚC ST1)

Leader (ThienPDM) chỉnh sửa nhỏ trên contract LX12 đã merge:

| File | Change | Status |
|---|---|---|
| `lib/features/streak/data/streak_repository.dart` | **NEW** — abstract `StreakRepository` với `watchUserMonth(uid, month)` + `getUserAllDates(uid)` | ❌ thêm |
| `lib/features/streak/application/streak_controller.dart` | **UPDATE schema** — `StreakState` thêm `errorMessage`, controller có `init/swipePrev/swipeNext/clearError` stub throw UnimplementedError, expose pure fn `calculateStreak(dates, now)` | ❌ rewrite |
| `lib/features/streak/presentation/streak_photo_detail_screen.dart` | **DELETE** stub — reuse shared `PhotoDetailScreen` | ❌ delete |
| `lib/core/router/app_router.dart` | Bỏ route `/streak/photo/:postId` (push trực tiếp PhotoDetailScreen, không cần route) | ❌ update |

> **Cross-module touch (GATE):** PR ST3 sẽ move `PhotoDetailScreen` + `SharePhotoSheet` từ `features/profile/` → `lib/shared/widgets/`. Owner HanDHG own cả Profile + Streak nên đỡ friction. Cần leader pre-approve để Profile tests không bị break.

---

## PART 2 — Tasks

---

### ST1 · feat(streak): StreakRepository + FirebaseStreakRepository

| | |
|---|---|
| Assignee | HanDHG |
| Estimate | S (~4h) |
| Branch | `feat/HanDHG/streak-repository` (sub-branch của `feature/HanDHG/streak-module`) |
| Blocked by | Part 1 contracts |

**Files:**
- `apps/mobile/lib/features/streak/data/streak_repository.dart` — full abstract interface
- `apps/mobile/lib/features/streak/data/firebase_streak_repository.dart` — full impl
- `apps/mobile/lib/features/streak/application/streak_providers.dart` — `streakRepositoryProvider` throws UnimplementedError stub (override trong main.dart)
- `apps/mobile/lib/main.dart` — override `streakRepositoryProvider.overrideWithValue(FirebaseStreakRepository(firestore: FirebaseFirestore.instance))`
- `apps/mobile/test/features/streak/data/firebase_streak_repository_test.dart` — unit tests

**Acceptance criteria:**
- [ ] `watchUserMonth(uid, month)` query đúng: `WHERE authorId == uid AND spaceId == null AND createdAt >= startOfMonthLocal AND createdAt < startOfNextMonthLocal ORDER BY createdAt ASC`
- [ ] `getUserAllDates(uid)` query `WHERE authorId == uid AND spaceId == null ORDER BY createdAt DESC` → map `toLocal()` → dedupe per day → return `List<DateTime>`
- [ ] FirebaseException mapped → AppError tại boundary (pattern AUTH #2)
- [ ] `flutter test test/features/streak/data/` — 0 failures
- [ ] `flutter analyze --no-pub` clean

**Test cases (fake_cloud_firestore):**
```dart
test('watchUserMonth trả posts trong khoảng + spaceId==null only', () async {
  // seed 5 posts: 2 trong tháng + spaceId=null, 1 trong tháng + spaceId!=null,
  // 1 ngoài tháng, 1 author khác → expect chỉ 2 posts emit
});
test('watchUserMonth tháng không có post → empty list', () async { ... });
test('getUserAllDates dedupe per day (post 8:00 + 20:00 cùng ngày → 1 entry)', () async { ... });
test('getUserAllDates filter spaceId==null', () async { ... });
test('Firestore PERMISSION_DENIED → throw ForbiddenError', () async { ... });
```

**Cross-module imports:** `Post` từ `features/feed/data/post.dart`, `AppError` từ `core/error/app_error.dart`.

---

### ST2 · feat(streak): StreakController + pure calculateStreak

| | |
|---|---|
| Assignee | HanDHG |
| Estimate | M (~6h) |
| Branch | `feat/HanDHG/streak-controller` |
| Blocked by | ST1 |

**Files:**
- `apps/mobile/lib/features/streak/application/streak_state.dart` — `@freezed` StreakState (theo spec §Controller pattern)
- `apps/mobile/lib/features/streak/application/streak_controller.dart` — full impl
- `apps/mobile/lib/features/streak/application/calculate_streak.dart` — pure fn `int calculateStreak(List<DateTime>, DateTime now)`
- `apps/mobile/test/features/streak/application/calculate_streak_test.dart` — pure fn tests
- `apps/mobile/test/features/streak/application/streak_controller_test.dart` — controller state transitions

**Acceptance criteria:**
- [ ] `build()` returns `const StreakState()` + register `ref.onDispose(() => _monthSub?.cancel())`
- [ ] `init()` set `viewingMonth = startOfCurrentMonthLocal`, subscribe `watchUserMonth`, fetch `getUserAllDates` once, compute `currentStreak`
- [ ] `swipePrev()` decrement `viewingMonth` 1 tháng, cancel old sub, subscribe new month, `currentStreak`/postCount KHÔNG đổi
- [ ] `swipeNext()` early-return nếu `viewingMonth == startOfCurrentMonthLocal` (cap); else increment
- [ ] `clearError()` set `errorMessage: null` nếu hiện non-null
- [ ] Pure `calculateStreak`: cover today=1, gap=streak đứt, empty=0, timezone UTC→UTC+7
- [ ] `_afterFailure(e)` pattern AUTH #4 — map AppError, fallback "Không thể tải Kỷ niệm"
- [ ] `flutter test test/features/streak/application/` — 0 failures, ≥ 80% coverage controller + ≥ 95% pure fn
- [ ] `flutter analyze` clean

**Test cases:**
```dart
// calculate_streak_test.dart
test('streak [today] → 1', () { ... });
test('streak [today, yesterday, 2d ago] → 3', () { ... });
test('streak [today, 3d ago] → 1 (gap)', () { ... });
test('streak [] → 0', () { ... });
test('timezone: post 2026-05-22T23:00Z, nowLocal UTC+7 = 2026-05-23T06:00 → dayKey 2026-05-23', () { ... });
test('streak [yesterday only] (no today) → 0', () { ... });
test('streak [today, yesterday × duplicates same day] → 2', () { ... });

// streak_controller_test.dart (ProviderContainer + mock StreakRepository)
test('init: load month posts + allPostDates + compute currentStreak', () async { ... });
test('swipePrev → viewingMonth - 1 month, monthPosts refetch, currentStreak unchanged', () async { ... });
test('swipeNext khi viewing tháng hiện tại → no-op, không gọi repo', () async { ... });
test('init fail → errorMessage set, isLoading false', () async { ... });
test('clearError → errorMessage null', () async { ... });
```

**Cross-module imports:** `streakRepositoryProvider` (ST1), `currentUidProvider` từ auth (chỉ dùng để get uid trong controller).

---

### ST3 · chore(shared): move PhotoDetailScreen + SharePhotoSheet → shared/ + parameterize + wire logic

| | |
|---|---|
| Assignee | HanDHG (own Profile + Streak) |
| Estimate | M (~6h) |
| Branch | `chore/HanDHG/shared-photo-detail` |
| Blocked by | (leader pre-approve gate change) |

**Files (move + edit):**
- `apps/mobile/lib/shared/widgets/photo_detail_screen.dart` — moved từ `features/profile/presentation/photo_detail_screen.dart`
- `apps/mobile/lib/shared/widgets/share_photo_sheet.dart` — moved từ `features/profile/presentation/widgets/share_photo_sheet.dart`
- `apps/mobile/lib/features/profile/presentation/profile_screen.dart` — update import path
- `apps/mobile/test/shared/widgets/photo_detail_screen_test.dart` — moved + new param tests
- `apps/mobile/test/shared/widgets/share_photo_sheet_test.dart` — new tests cho wired logic

**Acceptance criteria — PhotoDetailScreen:**
- [ ] Move file, update package path
- [ ] Thêm 4 optional params với default match current Profile behavior:
  ```dart
  final String? yearLabel;        // default: createdAt.year.toString()
  final Color captionPillColor;   // default: const Color(0x80000000)
  final Color timeColor;          // default: AppColors.bw100
  final ValueChanged<Post>? onShareTap;  // default: SharePhotoSheet.show với isAuthor=true, no Post
  ```
- [ ] Profile import path update: `package:meep/shared/widgets/photo_detail_screen.dart`
- [ ] Profile tests pass không thay đổi snapshot (defaults match)
- [ ] Widget test cover Streak invocation: inject `captionPillColor: Color(0x66394041)`, `timeColor: AppColors.bw600` → render đúng

**Acceptance criteria — SharePhotoSheet:**
- [ ] Move file, update package path
- [ ] Constructor: `required Post post`, `required bool isAuthor`
- [ ] "Chia sẻ" target → `Share.shareUri(Uri.parse(post.coverImageUrl))` + pop
- [ ] "Lưu" action → `http.get(coverImageUrl)` → `Gal.putImageBytes(response.bodyBytes)` + pop + toast "Đã lưu ảnh"; fail → toast "Không thể lưu, thử lại"
- [ ] "Xoá" action → hiện chỉ khi `isAuthor` → confirm dialog → `ref.read(postControllerProvider.notifier).deletePost(post.postId)` + pop sheet
- [ ] "Messenger" + "Instagram" → `url_launcher` deeplink → fallback browser nếu app chưa cài
- [ ] "Tin nhắn" → disabled (opacity 0.5, no onTap)
- [ ] Profile import path update
- [ ] Profile tests update để pass thêm `post: dummyPost, isAuthor: true` params

**Tests:**
```dart
// share_photo_sheet_test.dart
testWidgets('isAuthor=true → Xoá button visible', (tester) async { ... });
testWidgets('isAuthor=false → chỉ render Lưu, ẩn Xoá', (tester) async { ... });
testWidgets('tap Chia sẻ → Share.shareUri called', (tester) async { ... });
testWidgets('tap Lưu → Gal.putImageBytes called', (tester) async { ... });
testWidgets('tap Xoá → postControllerProvider.deletePost called', (tester) async { ... });
```

**Verification:**
- [ ] `flutter test test/features/profile/` — 0 failures
- [ ] `flutter test test/shared/widgets/` — 0 failures
- [ ] `flutter analyze` clean
- [ ] Manual: run Profile flow trên emulator → Photo detail mở + share sheet hoạt động

**Cross-module touch:** PR ghi rõ leader-gated. KHÔNG thay đổi behavior Profile, chỉ relocate.

---

### ST4 · feat(streak): StreakScreen — calendar + stats pill + empty state

| | |
|---|---|
| Assignee | HanDHG |
| Estimate | L (~12h, 1.5 ngày) |
| Branch | `feat/HanDHG/streak-screen` |
| Blocked by | ST2, ST3 |

**Files:**
- `apps/mobile/lib/features/streak/presentation/streak_screen.dart` — main screen
- `apps/mobile/lib/features/streak/presentation/widgets/streak_calendar.dart` — custom GridView 7×N
- `apps/mobile/lib/features/streak/presentation/widgets/calendar_day_cell.dart` — 3 states: post / no-post / today
- `apps/mobile/lib/features/streak/presentation/widgets/streak_stats_pill.dart` — pill bottom
- `apps/mobile/lib/features/streak/presentation/widgets/empty_state_overlay.dart` — subtitle + 2 arrow vectors
- `apps/mobile/test/features/streak/presentation/streak_calendar_test.dart`
- `apps/mobile/test/features/streak/presentation/streak_screen_test.dart`

**Acceptance criteria — Layout (Figma 269:1983):**
- [ ] Bg `#0d0804`
- [ ] Topbar: "Kỷ niệm" center Nunito Bold 18 white + avatar 40 góc phải bg `#73716f` (placeholder — load từ currentUserProfile.avatarUrl)
- [ ] Calendar container: cornerRadius 22, fill `#5857546e`, padding bottom 10
- [ ] Month header: "tháng MM YYYY" Inter SemiBold 14 white, bg `#8383836e` cornerRadius top inherit
- [ ] Grid 7 columns, cell 37×35, padding 4px giữa cells (compute từ Figma layout)
- [ ] Cell có post → CachedNetworkImage cornerRadius 7 fit cover
- [ ] Cell không có post → Center dot 11×11 cornerRadius 7 fill `#5857546e`
- [ ] Cell hôm nay → border 1px `#656565` + fill `#c4c4c4` overlay (dùng RoundedRectangleBorder)
- [ ] Pill stats: bg `#5857546e`, cornerRadius 12, padding 8/11, content " N Khoảnh khắc | Xd chuỗi" — N từ `currentUserProfileProvider.postCount`, X từ `streakControllerProvider.currentStreak`

**Acceptance criteria — Behavior:**
- [ ] Swipe phải→trái (gesture detector) → `controller.swipePrev()`
- [ ] Swipe trái→phải khi viewing tháng hiện tại → bounce animation, no-op
- [ ] Swipe trái→phải khi viewing tháng cũ → `controller.swipeNext()`
- [ ] Tap cell có post → push `PhotoDetailScreen` (shared) với `posts: state.monthPosts`, `initialIndex: tappedIndex`, `captionPillColor: Color(0x66394041)`, `timeColor: AppColors.bw600`, `onShareTap: (p) => SharePhotoSheet.show(ctx, post: p, isAuthor: true)`
- [ ] Tap cell không có post → no-op
- [ ] Empty state (state.allPostDates.isEmpty + state.monthPosts.isEmpty):
  - [ ] Subtitle "Gửi khoảnh khắc đầu tiên của bạn tại Meep !" Nunito Bold 14 `#ffffff7a` center
  - [ ] 2 arrow vectors render qua CustomPainter (Vector 3 + Vector 4 từ Figma `269:1988` + `269:1991`)
  - [ ] Pill hiện "0 Khoảnh khắc | 0d chuỗi"
- [ ] Loading: state.isLoading → skeleton calendar (placeholder grey cells)
- [ ] Error: state.errorMessage != null → banner top với errorMessage + tap Refresh → `controller.init()`

**Widget tests:**
```dart
testWidgets('Calendar render đúng số ngày trong tháng', (tester) async { ... });
testWidgets('Ngày có post → CachedNetworkImage', (tester) async { ... });
testWidgets('Ngày không có post → dot 11×11', (tester) async { ... });
testWidgets('Ngày hôm nay → border outline', (tester) async { ... });
testWidgets('Tap ngày có post → push PhotoDetailScreen', (tester) async { ... });
testWidgets('Tap ngày không có post → no-op', (tester) async { ... });
testWidgets('Swipe trái→phải tại tháng hiện tại → no swipeNext call', (tester) async { ... });
testWidgets('Empty state → subtitle + 2 arrow visible', (tester) async { ... });
testWidgets('StreakStatsPill render N + Xd format', (tester) async { ... });
```

**Verification:**
- [ ] `flutter test test/features/streak/presentation/` — 0 failures
- [ ] `flutter analyze` clean
- [ ] Manual emulator: navigate từ Taskbar tab 1 → calendar render → swipe tháng → tap ngày → photo detail → share/save/delete

**Cross-module imports:** `streakControllerProvider`, `currentUserProfileProvider`, `Post`, shared `PhotoDetailScreen` + `SharePhotoSheet`, `AppColors`, `AppTextStyles`.

---

### ST5 · chore(streak): wire route + Taskbar tab + integration test

| | |
|---|---|
| Assignee | HanDHG |
| Estimate | S (~4h) |
| Branch | `feat/HanDHG/streak-wiring` |
| Blocked by | ST4 |

**Files:**
- `apps/mobile/lib/core/router/app_router.dart` — route `/streak` → `StreakScreen()`, KHÔNG có sub-route photo (push trực tiếp PhotoDetailScreen từ StreakScreen)
- `apps/mobile/lib/shared/widgets/app_taskbar.dart` — wire tab calendar-days → navigate `/streak` (update existing widget)
- `apps/mobile/integration_test/streak_flow_test.dart` — E2E happy path

**Acceptance criteria:**
- [ ] Tap tab calendar-days trên Taskbar (bất cứ màn home/profile/chat/streak nào) → navigate `/streak`
- [ ] Đang ở `/streak` → tab calendar-days active state (icon white `#f9fcfc`)
- [ ] Route `/streak/photo/:postId` REMOVED (không cần — PhotoDetailScreen push qua MaterialPageRoute)
- [ ] Integration test: login fake user có 3 posts trong tháng → mở Streak → swipe back tháng cũ → empty state → swipe forward → tap ngày → photo detail → tap share → sheet hiện

**Verification:**
- [ ] `flutter test integration_test/streak_flow_test.dart` — pass
- [ ] `flutter analyze` clean
- [ ] Manual: cold start app → login → tab calendar-days → đúng màn Streak

---

## PART 3 — Dependency graph

```
contracts Part 1 (leader) → ST1 (Repository) → ST2 (Controller)
                                                ↘
                                          ST3 (shared widgets) → ST4 (Screen) → ST5 (wiring)
```

**Parallel possible:**
- ST3 có thể start ngay sau Part 1 (không depend ST1/ST2) — leader-gated PR.
- ST1 + ST3 → có thể chạy song song nếu owner muốn (HanDHG own cả 2).

**Estimated total:** ~32h ≈ 4 ngày làm việc full-time.

---

## PART 4 — Definition of Done (toàn module)

- [ ] Tất cả 5 ST tasks complete, PR merged vào `develop` qua review leader
- [ ] `flutter analyze --no-pub` clean
- [ ] `dart format --set-exit-if-changed --output none lib test` clean
- [ ] `flutter test` — 0 failures (cả unit + widget + integration)
- [ ] Coverage targets: pure fn ≥ 95%, controller ≥ 80%, repository ≥ 70%, widget ≥ 50%
- [ ] Manual smoke test trên emulator Android: cold start → Taskbar → Streak → swipe tháng → tap ngày → carousel → share → save → delete → ô tắt sáng
- [ ] OQ4 (`CaptionType.streak` integration) và OQ5 (milestone notification) defer post-MVP — note vào spec, không block release
- [ ] Update `tasks/todo-streak.md` checklist 100%

---

## PART 5 — Open questions (defer, NOT blocking)

| # | Câu hỏi | Recommendation |
|---|---|---|
| OQ4 | `CaptionType.streak` integration vào Feed CaptionService | **Defer post-MVP** — caption type 7 giữ mock trong Feed. Streak module không expose API trong MVP. |
| OQ5 | Milestone notification (7/30/100 ngày) | **Defer post-MVP** — Notification spec không thay đổi. Thêm CF `onStreakMilestone` ở milestone tiếp theo nếu cần. |
| OQ7 | `getUserAllDates` cost khi user >1000 posts | **Monitor** — chấp nhận MVP. Post-MVP: lưu `currentStreakDays` + `lastPostDate` vào `/users/{uid}` denormalized. |

---

> Streak KHÔNG có Firestore collection riêng — read-only từ `/posts`. Không cần rules mới, không cần Cloud Function.
