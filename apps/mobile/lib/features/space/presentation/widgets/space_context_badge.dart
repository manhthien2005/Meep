import 'package:flutter/material.dart';

// TODO(SP/T9/TBD): implement SpaceContextBadge per Figma
class SpaceContextBadge extends StatelessWidget {
  const SpaceContextBadge({super.key, required this.spaceId});

  final String spaceId;

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
