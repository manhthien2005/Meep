import 'package:flutter/material.dart';

// TODO(SP/T7/TBD): implement SpaceContextBottomSheet per Figma
class SpaceContextBottomSheet extends StatelessWidget {
  const SpaceContextBottomSheet({super.key, required this.spaceId});

  final String spaceId;

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
