import 'package:flutter/material.dart';

// TODO(T10/HanDHG): implement SignUpUsernamePage theo Figma — Đăng ký_Nhập username
class SignUpUsernamePage extends StatelessWidget {
  const SignUpUsernamePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SignUp Username — TODO')),
    );
  }
}
