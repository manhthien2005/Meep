import 'package:flutter/material.dart';

// TODO(ST/T4/TBD): implement StreakPhotoDetailScreen per Figma
class StreakPhotoDetailScreen extends StatelessWidget {
  const StreakPhotoDetailScreen({super.key, required this.postId});

  final String postId;

  @override
  Widget build(BuildContext context) => const Scaffold(body: SizedBox.shrink());
}
